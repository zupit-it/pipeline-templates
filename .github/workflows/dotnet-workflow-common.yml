name: .NET common

on:
  workflow_call:
    inputs:
      WORKING_DIRECTORY:
        required: true
        type: string
      DOTNET_IMAGE:
        required: true
        type: string
      CONTAINER_CI_LABELS:
        required: true
        type: string
      CSHARPIER_VERSION:
        required: false
        type: string
        default: ''

jobs:
  dotnet-common:
    name: Run .NET build, check formatting and test
    runs-on: ${{ fromJson(inputs.CONTAINER_CI_LABELS) }}
    container:
      image: ${{ inputs.DOTNET_IMAGE }}

    defaults:
      run:
        working-directory: ${{ inputs.WORKING_DIRECTORY }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Check code formatting
        uses: zupit-it/pipeline-templates/.github/actions/dotnet/format@v1.8.1
        with:
          WORKING_DIRECTORY: ${{ inputs.WORKING_DIRECTORY }}
          CSHARPIER_VERSION: ${{ inputs.CSHARPIER_VERSION }}

      - name: Build
        uses: zupit-it/pipeline-templates/.github/actions/dotnet/build@v1.8.1
        with:
          WORKING_DIRECTORY: ${{ inputs.WORKING_DIRECTORY }}
          BUILD_CONFIG: "Debug"

      - name: Run tests
        uses: zupit-it/pipeline-templates/.github/actions/dotnet/test@v1.8.1
        with:
          WORKING_DIRECTORY: ${{ inputs.WORKING_DIRECTORY }}
          GENERATE_CODE_COVERAGE: false
